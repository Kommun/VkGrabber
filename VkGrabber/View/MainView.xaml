<Page x:Class="VkGrabber.View.MainView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:VkGrabber.View"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300">

    <StackPanel>
        <TextBlock Text="Названия групп (каждое в новой строке)"/>
        <TextBox Text="{Binding VkSettings.GroupsToGrab, Mode=TwoWay}"
                 AcceptsReturn="True"/>

        <TextBlock Text="Название целевой группы (куда будут добавляться посты)"
                   Margin="0,20,0,0"/>
        <TextBox Text="{Binding VkSettings.TargetGroup, Mode=TwoWay}"/>

        <Button Content="Получить список постов"
                Command="{Binding GrabCommand}"/>
    </StackPanel>
</Page>
